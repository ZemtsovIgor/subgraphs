{
  "name": "voda",
  "description": "VODA Subgraph",
  "version": "1.0.0",
  "repository": "git@github.com:voda/thegraph.git",
  "author": "VODA",
  "license": "MIT",
  "scripts": {
    "codegen": "graph codegen subgraph.yaml",
    "build": "graph build subgraph.yaml",
    "deploy": "graph deploy --product hosted-service --node https://api.thegraph.com/deploy/ --ipfs https://api.thegraph.com/ipfs/ zemtsovigor/voda subgraph.yaml"
  }
}
